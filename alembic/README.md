# Database migrations

Alembic will be configured here when PostgreSQL authentication is implemented.
No migration is active yet, so the current RAG application does not require a
database connection.
